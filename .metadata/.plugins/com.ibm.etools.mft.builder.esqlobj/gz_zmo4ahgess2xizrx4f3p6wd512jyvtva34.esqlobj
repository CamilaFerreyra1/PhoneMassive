CREATE PROCEDURE ToCamel(IN refField REFERENCE)
BEGIN
	DECLARE firstLetter CHARACTER SUBSTRING(FIELDNAME(refField) FROM 1 FOR 1);
	SET refField NAME = LOWER(firstLetter) || RIGHT(FIELDNAME(refField), LENGTH(FIELDNAME(refField)) - 1);
END;